name: costco_price_tag_parser
description: >
  Heuristic parser that turns OCR text lines from a Costco A-format price tag
  into structured PriceTagData. Pure Dart, OCR-engine-agnostic.
version: 0.1.0
# repository: https://github.com/<owner>/costco-price-tag-parser

environment:
  sdk: ^3.0.0

# No runtime dependencies — the parser is pure Dart.

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0
