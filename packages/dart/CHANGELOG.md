# Changelog

## 0.1.0

- Initial release. `parsePriceTag(List<OcrLine>) → PriceTagData` for Costco
  A-format rack cards, plus the `PriceTagData` / `OcrLine` model and the
  `OcrEngine` interface. Pure Dart, zero runtime dependencies.
